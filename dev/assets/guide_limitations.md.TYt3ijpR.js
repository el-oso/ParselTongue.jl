import{_ as i,o as e,c as a,ao as t}from"./chunks/framework.C2uOknPB.js";const k=JSON.parse('{"title":"Limitations","description":"","frontmatter":{},"headers":[],"relativePath":"guide/limitations.md","filePath":"guide/limitations.md","lastUpdated":null}'),n={name:"guide/limitations.md"};function l(o,s,h,r,d,p){return e(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="Limitations" tabindex="-1">Limitations <a class="header-anchor" href="#Limitations" aria-label="Permalink to &quot;Limitations {#Limitations}&quot;">​</a></h1><p>ParselTongue is built on the experimental <code>juliac --trim</code> (Julia ≥ 1.12). These are the known constraints, with the reasoning behind each.</p><h2 id="One-extension-per-Python-process" tabindex="-1">One extension per Python process <a class="header-anchor" href="#One-extension-per-Python-process" aria-label="Permalink to &quot;One extension per Python process {#One-extension-per-Python-process}&quot;">​</a></h2><p>Each separately compiled extension embeds its own trimmed Julia system image. That image&#39;s self-initialisation routine runs on <code>dlopen</code>; a second such routine in the same process aborts in <code>jl_init_threadtls</code>. This means two independent ParselTongue <code>.so</code> / <code>.pyd</code> files cannot coexist in one Python process, <strong>regardless of whether <code>runtime=:bundled</code>, <code>:shared</code>, or <code>:system</code> is used</strong>:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> mathx     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ok — first .so loads, Julia self-initialises</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> strx      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># SIGABRT — second .so tries to self-initialise Julia again</span></span></code></pre></div><p>The fix is always the same: put everything behind <strong>one</strong> compiled extension and split the API at the Python level:</p><ul><li><p><strong>Within one source file:</strong> use <a href="/ParselTongue.jl/dev/examples/scientific#Submodules"><code>@pymodule pkg.sub</code></a> to expose submodules (<code>pkg.linalg</code>, <code>pkg.dsp</code>, …), all backed by one image, so <code>import pkg.linalg</code> and <code>import pkg.dsp</code> coexist fine.</p></li><li><p><strong>Across several source files:</strong> <a href="/ParselTongue.jl/dev/reference/api#Building"><code>build_multi_wheel([&quot;a.jl&quot;, &quot;b.jl&quot;], &quot;pkg&quot;)</code></a> aggregates them into one extension and exposes each file as a submodule (<code>pkg.a</code>, <code>pkg.b</code>) — they import and run together in one process. Function names must be unique across the files (they share one C method table).</p></li></ul><p>Removing this limit would require a juliac &quot;link-only&quot; trim mode that emits <code>@ccallable</code> stubs without embedding a self-init — a Julia toolchain change outside ParselTongue&#39;s scope.</p><h2 id="Wheel-size-100-MB" tabindex="-1">Wheel size (~100 MB) <a class="header-anchor" href="#Wheel-size-100-MB" aria-label="Permalink to &quot;Wheel size (~100 MB) {#Wheel-size-100-MB}&quot;">​</a></h2><p>The trimmed code is tiny, but the Julia runtime&#39;s standard-library <code>__init__</code> functions (OpenBLAS, SuiteSparse, …) run at startup and dlopen their backing libraries even when your code never uses them. Those libraries must therefore be bundled. Only the system image, the LLVM JIT, and codegen (~500 MB) are excluded, since a trimmed AOT binary provably never needs them.</p><p>Two options already reduce or remove this: <code>slim=true</code> vendors only the libraries reachable via <code>DT_NEEDED</code> (~38 MB, safe when your code uses no stdlib JLLs such as <code>LinearAlgebra</code>), and <code>runtime=:shared</code> / <code>runtime=:system</code> skip vendoring entirely (~1 MB wheel, with the Julia runtime installed once and shared by every extension). Slimming the <em>default</em> bundle further — by suppressing the unused stdlib inits — is still a planned optimization.</p><h2 id="Array-dtype-checking-is-width-only" tabindex="-1">Array dtype checking is width-only <a class="header-anchor" href="#Array-dtype-checking-is-width-only" aria-label="Permalink to &quot;Array dtype checking is width-only {#Array-dtype-checking-is-width-only}&quot;">​</a></h2><p>An array argument is validated by element <strong>size</strong>, not signedness or kind. A <code>float64</code> buffer passed where <code>Int64</code> is expected (both 8 bytes) is not caught and will be reinterpreted. Match dtypes carefully on the Python side.</p><h2 id="trim-:safe-rejects-dynamic-dispatch" tabindex="-1"><code>trim = :safe</code> rejects dynamic dispatch <a class="header-anchor" href="#trim-:safe-rejects-dynamic-dispatch" aria-label="Permalink to &quot;\`trim = :safe\` rejects dynamic dispatch {#trim-:safe-rejects-dynamic-dispatch}&quot;">​</a></h2><p>This is a feature, not a bug: type-unstable or dynamically-dispatched code in an exported path fails the build. See <a href="/ParselTongue.jl/dev/guide/building#Trim-modes">Building</a> for the <code>:unsafe_warn</code> escape hatch.</p><h2 id="Rust-style-Result/Option-types" tabindex="-1">Rust-style Result/Option types <a class="header-anchor" href="#Rust-style-Result/Option-types" aria-label="Permalink to &quot;Rust-style Result/Option types {#Rust-style-Result/Option-types}&quot;">​</a></h2><p><a href="https://github.com/jakobnissen/ErrorTypes.jl" target="_blank" rel="noreferrer">ErrorTypes.jl</a> and similar <code>Result{T,E}</code> / <code>Option{T}</code> libraries work fine <strong>inside</strong> Julia helper functions — the monadic operators (<code>@?</code>, <code>and_then</code>, <code>map</code>) are pure Julia and invisible to the boundary. The <code>@pyfunc</code> that crosses into Python must unwrap to a boundary type:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ErrorTypes</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_safe_div_impl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Float64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, b</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Float64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Result{Float64,String}</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    b </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Err</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;division by zero&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Ok</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@pyfunc</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> safe_div</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Float64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, b</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Float64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Float64</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> begin</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    r </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> _safe_div_impl</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    is_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(r) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unwrap_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(r)) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unwrap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(r)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>From Python&#39;s perspective, <code>Err(...)</code> and <code>throw(...)</code> are identical — both become Python exceptions. <code>Union{T,Nothing}</code> already covers <code>Option{T}</code> if you want an optional return. ErrorTypes.jl is not a ParselTongue dependency and not needed for any boundary feature; it is purely an ergonomic choice inside your Julia logic.</p>`,19)])])}const u=i(n,[["render",l]]);export{k as __pageData,u as default};
